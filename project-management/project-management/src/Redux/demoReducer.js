
const initialState={
    user:null,
    loading:false,
    error:null,
    jwt:null,
    projectSize:0,
}

export const projectReducer = (state=initialState, action)=>{
    switch (action.type) {
            
        default:
            state;
    }
}